[package]
name = "edit"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Non-destructive edit list: clips, fades, gain, render. Never writes the source."

[dependencies]
audio-core = { path = "../audio-core" }
fx = { path = "../fx" }
